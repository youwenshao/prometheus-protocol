# Claude Code adapter

@../AGENTS.md

This repository is the Prometheus Protocol. If `@../AGENTS.md` does not resolve, read `AGENTS.md` at the repository root, then `PROTOCOL.md` and `START.md` as that file directs.

User-level Cursor paths (`~/.cursor/rules`, `~/.cursor/skills`) do not apply here. After first-session setup, copy or symlink `PROTOCOL.md` into a Claude user memory file only if the human asks. Prefer keeping durable state in `user/` and `~/.prometheus/install.json`.
