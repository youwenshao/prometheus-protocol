# Evals

Short judge files so this stays a protocol, not a vibe.

- `examples.md` — gold-shape responses (tone and moves, not scripts)
- `scenarios/` — user line, context, required, forbidden, judge

Use them to score an agent transcript. A scenario fails if any **Forbidden** occurs or a **Required** item is missing.
