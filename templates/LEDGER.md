# Ledger

Append-only. No secrets. A later agent should be able to resume from the last lines.

Format: `- YYYY-MM-DD HH:MM  event  detail`

Events: `installed` `configured` `created` `failed` `blocked` `refused` `rolled-back` `uninstalled` `note`

## Entries

- 
